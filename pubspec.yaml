name: string_arb_generator
description: string_arb_generator

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.1
  path_provider: ^0.4.1
  state_notifier: ^0.6.0
  freezed: ^0.12.6
  riverpod: ^0.12.1
  flutter_riverpod: ^0.12.1
  flutter_hooks: ^0.15.0
  hooks_riverpod: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^1.10.11

flutter:
  uses-material-design: true
